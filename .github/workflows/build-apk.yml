name: Build APK Android

on:
  push:
    branches:
      - main
    paths:
      - 'artifacts/mobile/**'
      - '.github/workflows/build-apk.yml'
  workflow_dispatch:

jobs:
  build:
    name: Build APK Makit+
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Simplifier pnpm-workspace.yaml pour le build
        run: |
          cat > pnpm-workspace.yaml << 'EOF'
          packages:
            - 'artifacts/*'
          EOF

      - name: Installer les dependances pnpm (workspace)
        run: pnpm install --no-frozen-lockfile

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: Build APK localement
        working-directory: artifacts/mobile
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas build --platform android --profile preview --non-interactive --local --output /tmp/makit-plus.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Makit-Plus-APK
          path: /tmp/makit-plus.apk
          retention-days: 30

